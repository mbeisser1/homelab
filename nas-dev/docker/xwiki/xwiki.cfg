# Disable clustering features (not needed for single user)
xwiki.cluster=0

# Increase document cache
xwiki.store.cache.documents.size=5000

# Disable watch service for faster startup
xwiki.store.watch=0
