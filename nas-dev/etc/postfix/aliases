# /etc/aliases
# Default aliases to keep the system happy

# Root/admin mail → your Fastmail address
root: nas-dev@bitrealm.dev

# RFC-required postmaster → goes to root (which we already redirect above)
postmaster: root

# System accounts redirected to root
abuse: root
hostmaster: root
webmaster: root

# Local user → Fastmail address
mbeisser: nas-dev@bitrealm.dev
