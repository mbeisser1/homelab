# /etc/aliases
# Default aliases to keep the system happy

# Root/admin mail → your Fastmail address
root: nas@bitrealm.dev

# RFC-required postmaster → goes to root (which we already redirect above)
postmaster: root

# You can add others if you want (optional)
abuse: root
hostmaster: root
webmaster: root
mbeisser: nas@bitrealm.dev
